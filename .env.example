# Docker Compose 환경 변수 템플릿
# 이 파일을 복사해서 .env 파일로 만들고 실제 값을 입력하세요
# cp .env.example .env

# Spring Profile
SPRING_PROFILES_ACTIVE=dev

# MySQL 설정 (Auth Service)
MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=test_panda_auth
MYSQL_USER=testpanda
MYSQL_PASSWORD=password

# PostgreSQL 설정 (Core Service)
POSTGRES_DB=test_panda_core
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password

# RabbitMQ 설정
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=admin

# JWT 설정 (Auth Service)
JWT_SECRET=test-panda-secret-key-change-in-production-please

# CORS 설정 (API Gateway)
ALLOWED_ORIGINS=http://localhost:3000
