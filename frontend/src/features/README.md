# 🔵 Features Layer

## 역할
사용자 시나리오와 비즈니스 기능을 구현하는 레이어입니다.

## 포함 내용
- `auth/` - 로그인, 로그아웃, 회원가입
- `exam-crud/` - 시험 생성, 수정, 삭제
- `question-editor/` - 문제 편집 기능
- `answer-submit/` - 답안 제출 기능
- `grading/` - 채점 기능

## 예시
```
features/
├── auth/
│   ├── ui/        # 기능 UI (로그인 폼 등)
│   ├── model/     # 기능 상태
│   ├── api/       # 기능 API
│   └── lib/       # 기능 로직
└── exam-crud/
    └── ...
```

## 의존성 규칙
- entities, shared에 접근 가능
- widgets, pages, processes, app에서 접근 가능
